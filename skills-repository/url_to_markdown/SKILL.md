---
name: url_to_markdown
description: Fetches a URL and converts the content to markdown preserving structure
schema_source: url_to_markdown.py
---

# Url To Markdown

## Description

Fetches a URL and converts the content to markdown preserving structure

## Usage

This skill is a self-documenting CLI tool. All files must be in the new folder with the name of the domain.

### Discovery

Get the JSON schema for inputs:

```bash
python skills/url_to_markdown/url_to_markdown.py --schema
```

### Execution

```bash
python skills/url_to_markdown/url_to_markdown.py --query "input"
```

## Executable

Path: `skills/url_to_markdown/url_to_markdown.py`
